'use strict';

export default class Tool {
    // artwork should be a Store object
    constructor(artwork) {
        this.artwork = artwork;
    }

    update() {}

    onMousePress(mouseX, mouseY) {

    }

    onMouseMove(mouseX, mouseY) {

    }

    onMouseRelease(mouseX, mouseY) {

    }
}